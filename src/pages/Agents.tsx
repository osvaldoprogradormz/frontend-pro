import{useAgents}from"../hooks/useAgents";import{StateView}from"../components/common/StateView";export default function Agents(){const q=useAgents();const rows=Array.isArray(q.data?.data)?q.data.data:[];return <section><h1 className="mb-6 text-2xl font-bold">AI Agents</h1>{q.data?.available===false?<StateView title="ENDPOINT NOT AVAILABLE" detail={q.data.error}/>:!rows.length?<StateView title="NO AGENTS"/>:<div className="grid gap-4 md:grid-cols-2">{rows.map((a,i)=><pre key={i} className="overflow-auto rounded-xl border border-slate-800 bg-vf-card p-4 text-xs">{JSON.stringify(a,null,2)}</pre>)}</div>}</section>}