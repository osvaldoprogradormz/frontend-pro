import{useQuery}from"@tanstack/react-query";import{api}from"../lib/api";import{StateView}from"../components/common/StateView";export default function History(){const q=useQuery({queryKey:["history"],queryFn:api.history});const data=Array.isArray(q.data?.data)?q.data.data:[];return <section><h1 className="mb-6 text-2xl font-bold">History</h1>{q.data?.available===false?<StateView title="ENDPOINT NOT AVAILABLE" detail={q.data.error}/>:!data.length?<StateView title="NO HISTORY"/>:<pre className="overflow-auto rounded-xl border border-slate-800 bg-vf-card p-5 text-xs">{JSON.stringify(data,null,2)}</pre>}</section>}