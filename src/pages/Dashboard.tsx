import{useCallback}from"react";import{useHealth}from"../hooks/useHealth";import{useWebSocket}from"../hooks/useWebSocket";import{StatusCard}from"../components/dashboard/StatusCard";import{StateView}from"../components/common/StateView";export default function Dashboard(){const h=useHealth();const ws=useWebSocket(useCallback(()=>{},[]));const online=h.data?.available===true;return <section><h1 className="text-2xl font-bold">Dashboard</h1><p className="mt-1 text-sm text-slate-500">Estado real do sistema.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><StatusCard label="Backend" online={h.isLoading?null:online}/><StatusCard label="Market Data" online={null}/><StatusCard label="Scanner" online={null}/><StatusCard label="WebSocket" online={ws==="CONNECTED"}/></div><div className="mt-6">{h.data?.available===false?<StateView title="BACKEND OFFLINE" detail={h.data.error}/>:<StateView title={h.data?.data?String((h.data.data as Record<string,unknown>).status??"Backend connected"):"Waiting for backend data"}/>}</div></section>}